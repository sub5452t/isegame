<!DOCTYPE html>
<html lang="th">
<head>
<meta charset="UTF-8">
<title>Isekai Dice : เกมจับโกหก</title>

<style>
body {
    margin: 0;
    font-family: system-ui, sans-serif;
    background: linear-gradient(135deg, #1f1c2c, #928dab);
    color: white;
    text-align: center;
}

.container {
    min-height: 100vh;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
}

img {
    width: 240px;
    border-radius: 20px;
    margin-bottom: 20px;
    box-shadow: 0 10px 30px rgba(0,0,0,.4);
}

h1 {
    font-size: 2.4rem;
    margin: 10px 0;
}

p {
    opacity: .9;
    margin-bottom: 30px;
}

button {
    width: 220px;
    padding: 14px;
    margin: 10px;
    font-size: 1.1rem;
    border-radius: 14px;
    border: none;
    cursor: pointer;
    background: #ffffff;
    color: #333;
    transition: transform .15s, box-shadow .15s;
}

button:hover {
    transform: translateY(-2px);
    box-shadow: 0 8px 20px rgba(0,0,0,.3);
}

.hidden {
    display: none;
}

input {
    padding: 10px;
    border-radius: 10px;
    border: none;
    font-size: 1rem;
    width: 200px;
    text-align: center;
}
</style>
</head>

<body>

<div class="container" id="home">
    <img id="gameImage" src="https://via.placeholder.com/300x300.png?text=ISEKAI+GAME">
    <h1>เกมจับโกหก</h1>
    <p>ร่วมมือหรือหลอกลวง เพื่อเอาชีวิตรอด</p>

    <button onclick="createRoom()">🎲 สร้างเกม</button>
    <button onclick="showJoin()">🚪 เข้าร่วมเกม</button>
</div>

<div class="container hidden" id="join">
    <h1>เข้าร่วมเกม</h1>
    <p>ใส่รหัสห้อง</p>
    <input id="roomCode" placeholder="เช่น 4821">
    <br><br>
    <button onclick="joinRoom()">เข้าเกม</button>
    <button onclick="backHome()">⬅ กลับ</button>
</div>

<script>
function createRoom() {
    const code = Math.floor(1000 + Math.random() * 9000);
    alert("🎉 สร้างห้องสำเร็จ!\nรหัสห้องคือ: " + code);
}

function showJoin() {
    document.getElementById("home").classList.add("hidden");
    document.getElementById("join").classList.remove("hidden");
}

function backHome() {
    document.getElementById("join").classList.add("hidden");
    document.getElementById("home").classList.remove("hidden");
}

function joinRoom() {
    const code = document.getElementById("roomCode").value;
    if(code.length < 4) {
        alert("❌ รหัสห้องไม่ถูกต้อง");
        return;
    }
    alert("✅ เข้าร่วมห้อง " + code + " สำเร็จ!");
}
</script>

</body>
</html>
